export const RESEND_CLIENT = Symbol('RESEND_CLIENT');

/** From-address used for all platform transactional emails. Configurable via env. */
export const DEFAULT_FROM_NAME    = 'BOS Platform';
export const DEFAULT_FROM_ADDRESS = 'noreply@bosplatform.com';
